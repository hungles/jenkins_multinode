configuration-as-code       # Enables managing Jenkins configuration through YAML files (JCasC)
workflow-job                # Provides the base Job type for Pipeline jobs
credentials                 # Core support for securely storing and using credentials
ssh-slaves                  # Allows Jenkins to launch agents via SSH
workflow-aggregator         # Installs the essential plugins for using Jenkins Pipelines
git                         # Integrates Jenkins with Git repositories
github                      # Adds GitHub integration and webhook support
credentials-binding         # Allows binding credentials (like secrets or passwords) into build steps
docker-plugin               # Enables builds to run inside Docker containers
blueocean                   # Provides a modern, user-friendly UI for Jenkins Pipelines
matrix-auth                 # Adds fine-grained role-based access control
pipeline-github-lib         # Allows loading shared pipeline libraries from GitHub
pipeline-stage-view         # Adds a visual view of pipeline stages and execution
ansicolor                   # Adds ANSI color support to console output
email-ext                   # Provides advanced email notifications based on build results
mailer                      # Basic email functionality for sending notifications
build-timeout               # Adds timeout options to abort long-running builds
timestamper                 # Adds timestamps to console output for better traceability
metrics                     # Provides system and performance metrics via REST endpoints